export interface AutoSpliceSettings {
  startSeconds: number;
  endSeconds: number;
  minDuration: number;
  maxDuration: number;
  minFrequency: number;
  maxFrequency: number;
  minAmplitude: number;
  maxAmplitude: number;
}